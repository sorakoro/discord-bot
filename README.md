# discord-bot

## Useage

Set the access token in an environment variable
```sh
export DISCORD_BOT_TOKEN=Your Access Token
```


Run the following command
```
go run main.go -c Your Channel ID -m Message
```
